<Window x:Class="Diary.Presentation.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Diary.Controls.Views;assembly=Diary.Controls"
        mc:Ignorable="d"
        Icon="{Binding LogoPath}"
        Title="MainWindow" Height="450" Width="800"
        WindowState="Maximized">

    <Window.Resources>
        <Style x:Key="Menu">
            <Setter Property="ColumnDefinition.MinWidth" Value="70"/>
            <Setter Property="ColumnDefinition.MaxWidth" Value="300"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Style="{StaticResource Menu}" Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <controls:MenuControl DataContext="{Binding MenuControlViewModel}"/>
        <Frame Grid.Column="1" Content="{Binding CenterPage}"/>

    </Grid>
</Window>
